## 2024-10-24 - Actionable Empty States
**Learning:** Found that zero-result empty states for filtering or searching lacked a recovery path, leaving users stuck if they over-filtered.
**Action:** Always include an actionable button (e.g., 'Clear Filters') in empty states that programmatically resets input values and re-triggers the list render to provide a clear and accessible recovery path.
